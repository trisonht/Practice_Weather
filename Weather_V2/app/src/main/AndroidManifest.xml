<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hieu_iceTea.weather_V2">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".controller.VolleyAppController"
        android:allowBackup="true"
        android:icon="@drawable/icon_launcher_weather"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_launcher_weather"
        android:supportsRtl="true"
        android:theme="@style/Theme.Weather_V2">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>